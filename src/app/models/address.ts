export class Address {
    id: number;
    name: string;
    firstline: string;
    secondline: string;
    district: string;
    city: string;
    postalCode: string;
    latitude: string;
    longitude: string;

    constructor(
    ) {
    }
}
